export const siteConfig = {
  name: "thejobs4u",
  logo: {
    // Kept for any non-<Logo> consumer that still wants a single image --
    // the six nav/header/footer sites use src/components/common/Logo.tsx
    // (icon image + coded text) instead, since this square lockup goes
    // illegible at the small fixed heights those render logos at.
    url: "/logo-icon.png",
    alt: "thejobs4u"
  },
  links: {
    whatsapp: "https://chat.whatsapp.com/example-group-link",
  }
};
